- if current_user
  %button=link_to "My Picks", :picks
%button=link_to "Standings", :standings
- if current_user && current_user.admin?
  %button=link_to "Users", :users
  %button=link_to "Team List", :teams
  
- if !current_user
  #login_notice You must login in order to select teams.  
:javascript
  today  = new Date();
  todayEpoch  = today.getTime();

  target = new Date("1 September, 2011"); 
  targetEpoch = target.getTime();

  daysLeft = Math.floor(((targetEpoch - todayEpoch) / (60*60*24)) / 1000);
  hoursLeft = (daysLeft % 24)

%h6
  %span
    Season starts in 
    :javascript
      document.write(daysLeft + " Days, " + hoursLeft + " Hours");
  %span . Are you ready?

=render 'warning'
-#=render 'todo'
=render 'rules'
=render 'money'
=render 'faq'