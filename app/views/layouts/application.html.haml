!!!
%html
  %head
    %title MovingGoalPost.com
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults, 'jquery.paginate'
    = csrf_meta_tag
  %body
    .wrapper
      #login_wrapper
        = render :partial => "layouts/header"
      #content
        #nav_wrapper
          %br
        #body_wrapper
          %br
        #content_header
        #content_body
          - if flash[:notice]
            .flash_notice= flash[:notice]
          - if flash[:error]
            .flash_error= flash[:error]
          = render :partial => "layouts/page_header" unless @skip_header
          .page_content  
            = yield
            %br
        #content_footer
      #footer
        &copy; 
        = Date.today.year 
        PmanCo
