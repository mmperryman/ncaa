=javascript_include_tag 'picker'

=link_to "Home", root_path
=link_to "Team List", :teams
%br
.header
  .subheader
    .wins_total
      Number of Teams Picked:
      #total_teams_picked= 0.to_s + " Teams Selected"
      Your Current Picks Total 2010 Wins:
      #prev_total_wins= current_user.prev_win_total
      Your Current Picks Total 2011 Wins:
      #current_total_wins= current_user.current_win_total
  - if @picks.empty?
    %div
      .no_picks You have NO picks yet!
  - else
    -@picks.each do |pick|
      .pick_team
        %span.pick_field= pick.team.name
        %span.pick_field= pick.team.short_name
        %span.pick_field= pick.team.mascot
        %span.pick_field= pick.team.prev_record.wins
="*"*50
=render 'form'

